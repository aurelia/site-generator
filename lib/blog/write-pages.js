exports.writePages = function(project) {
  //read in metadata for each post in the published folder
  //sort items by date descending
  //write configured number per page

  return Promise.resolve();
}
